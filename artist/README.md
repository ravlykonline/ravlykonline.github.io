# Ravlyk Artist — навчальний редактор алгоритмічного малювання

`Ravlyk Artist` — браузерна навчальна гра/редактор у стилі Code.org Artist: дитина складає програму з блоків, запускає її, а Равлик малює шлях на полі. Мета проєкту — створити простий, україномовний, безпечний і доступний інструмент для початкової школи без реєстрації та без збору персональних даних.

---

## 1. Поточний стан проєкту

Завершено Фазу 1 (P0) і Фазу 2 (P1).

У проєкті вже є:

- статичний сайт без backend;
- модульна ES-структура у `js/`;
- окремі CSS-файли у `css/`;
- 6 рівнів у `js/data/lessons.js` (всі українською, без XSS);
- блоки у `js/data/blocks.js` з українськими назвами;
- workspace з блоками та `repeat`;
- запуск програми з кнопкою `Зупинити`;
- canvas/SVG-візуалізація шляху Равлика;
- показ згенерованого псевдокоду (українською);
- execution limits (`js/runtime/execution-limits.js`);
- undo/redo (`js/state/history.js`);
- кнопки керування блоками (↑ ↓ Назовні Видалити);
- keyboard shortcuts (↑↓ Delete Ctrl+Z Ctrl+Y Ctrl+Enter Escape);
- focus trap у модальних вікнах;
- `package.json` з командою `npm test`;
- 23 unit-тести у `tests/art.core.test.js`.

---

## 2. Production-вхідні точки

Production-версія використовує:

```text
index.html
package.json
css/*.css
js/main.js
js/core/*.js
js/data/*.js
js/runtime/*.js
js/state/*.js
js/utils/*.js
js/ui/*.js
tests/*.js
```

Старі файли перенесено:

```text
legacy/script.legacy.js
legacy/style.legacy.css
```

---

## 3. Як запустити локально

Оскільки використовується `type="module"`, сайт треба відкривати через локальний сервер, а не подвійним кліком по `index.html`.

Варіант через Python:

```bash
python -m http.server 8000
```

Потім відкрити:

```text
http://localhost:8000
```

Варіант через Node:

```bash
npx serve .
```

---

## 4. Як запустити тести

```bash
npm test
```

або напряму:

```bash
node tests/art.core.test.js
```

У майбутньому треба додати:

```json
{
  "scripts": {
    "validate:levels": "node tools/validate-levels.js",
    "lint": "eslint js tests",
    "test:e2e": "playwright test"
  }
}
```

Дивись деталі в `TESTING.md`.

---

## 5. Цільовий функціонал першої стабільної версії

MVP дає дитині можливість:

1. вибрати рівень; ✅
2. бачити зрозуміле завдання українською; ✅
3. додавати блоки у програму кліком або перетяганням; ✅
4. використовувати повторення; ✅
5. запускати програму; ✅
6. зупиняти програму; ✅
7. бачити, як Равлик малює шлях; ✅
8. отримувати зрозумілий успіх або підказку; ✅
9. скидати спробу; ✅
10. переміщати блоки без drag-and-drop; ✅
11. скасовувати/повертати дії (undo/redo); ✅
12. переходити до наступного рівня; ✅
13. працювати без реєстрації; ✅
14. працювати без збереження персональних даних. ✅

---

## 6. Цільовий функціонал Artist-режиму

Поточна версія керує Равликом через напрямки `вгору/вниз/ліворуч/праворуч` (grid-режим). Для аналога Code.org Artist потрібно додати turtle-модель:

- `рухатися вперед`;
- `повернути ліворуч`;
- `повернути праворуч`;
- `повторити`;
- `стрибнути вперед` без малювання;
- `обрати колір`;
- `випадковий колір`;
- пізніше — функції.

Рекомендована прогресія:

1. **Режим клітинок** — найпростіше керування для молодших дітей. ✅
2. **Режим художника** — напрямок, повороти, перо, кольори.
3. **Режим візерунків** — повтори, вкладені повтори, обмеження кількості блоків.
4. **Режим функцій** — пізній етап, не для першого MVP.

---

## 7. Що залишилось до MVP

P2 — Artist/turtle-функціонал:

- turtle-engine (`js/domain/turtle.js`);
- команди `move_forward`, `turn_left`, `turn_right`;
- `jump_forward` (рух без малювання);
- `set_color`, `random_color`;
- segment-based goals;
- рівні 9–20.

P3 — Polish:

- `tools/validate-levels.js`;
- PWA (manifest, service worker, offline);
- security headers (`_headers`);
- CI (GitHub Actions).

Повний список — у `TASKS.md`.

---

## 8. Документи проєкту

Перед внесенням змін прочитати:

- `AGENTS.md` — правила роботи для Codex/Claude Code;
- `ARCHITECTURE.md` — цільова архітектура;
- `LEVELS.md` — формат і правила рівнів;
- `SECURITY.md` — безпека, CSP, дитячі дані;
- `ACCESSIBILITY.md` — доступність і keyboard-first UX;
- `TESTING.md` — тестування;
- `PWA.md` — offline/PWA;
- `TASKS.md` — backlog реалізації.

---

## 9. Принципи продукту

1. **Дитина має розуміти, що відбувається.** Кожна помилка має пояснюватися людською мовою.
2. **Не має бути страху експериментувати.** Є undo/redo, reset, stop.
3. **Без реєстрації.** Для шкільного використання це сильна перевага.
4. **Без персональних даних.** Не питати ім'я, клас, email.
5. **Offline-first.** Сайт має працювати навіть при нестабільному інтернеті.
6. **Не drag-only.** Усе важливе працює кліком, клавіатурою і на планшеті.
7. **Українська мова за замовчуванням.** Усі тексти інтерфейсу й рівнів українською.
8. **Модульність.** Логіка рівнів, engine, UI, state і rendering розділені.

---

## 10. Коротка команда для агента

Якщо агент починає з поточного стану:

1. Прочитати `AGENTS.md`.
2. Запустити `npm test` — переконатися, що всі тести зелені.
3. Взяти наступну незакриту задачу з `TASKS.md` (зараз це P2.1 — turtle-engine).
4. Не ламати існуючий запуск через `index.html` і `js/main.js`.
